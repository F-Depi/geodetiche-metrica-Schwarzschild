% ::setlocal makeprg=cd\ latex\ &&\ pdflatex\ -interaction=batchmode\ main.tex\ &&\ xdg-open\ main.pdf
Computed in \texttt{C} and animated in \texttt{python}
(hopefully, if I have enough time)

% ciaooooo \cite{eco2017come}

